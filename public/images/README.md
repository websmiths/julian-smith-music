# Images

Currently used:

- `julian-smith-portrait.jpg` — sidebar portrait on the home page (4:5 slot)
- `julian-smith-bass-abstract.jpg` — painterly hero strip on the About page (16:5 slot)

To add more:

- Per-band hero images — set the `image:` field in a band's frontmatter to e.g. `/images/the-magnificence.jpg`. 16:9 aspect (~1600×900) recommended.
- Anywhere else, just reference `/images/<filename>` in templates.
